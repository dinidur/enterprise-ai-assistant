[pytest]
# Put the project root on sys.path so tests can `import app...` without the
# package being installed. Without this, pytest only adds the tests directory.
pythonpath = .
asyncio_mode = auto
testpaths = tests
addopts = -q
